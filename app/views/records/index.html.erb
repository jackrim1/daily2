<p id="notice"><%= notice %></p>

<h1>Diary</h1>

<%= link_to image_tag("add.png", :size => "100x100"), new_record_path %>

<p>
<table>
  <thead>
    <tr>
      <th>Previous</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |record| %>
      <tr>
        <!-- <td><%= record.title %></td>
        <td><%= link_to 'Show', record %></td> -->
        <td><%= link_to record.title, edit_record_path(record) %></td>
        <!-- <td><%= link_to 'Destroy', record, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>


